namespace TimeTrack.Core.Models;

/// <summary>
/// The three tracked categories the UI encodes with colour
/// (green = work, amber = break, grey = idle).
/// </summary>
public enum SegmentType
{
    Work,
    Break,
    Idle
}
